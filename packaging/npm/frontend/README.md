# @tokenring-ai/one-frontend

The prebuilt TokenRing One web frontend assets. The package contents are under
`frontend/one`.

Most users should install `@tokenring-ai/one`, which includes the frontend,
backend, and terminal client.
